/* Arvore binária
  - os 'buracos' sempre estão no ultimo nível e a direita
  - na raiz sempre é o maior/menor número
  - o nó sempre é maior/menor que o seus filhos
  - lógica por profundidade
  - não é um algoritmo de busca O(n)
  - utilizar o maior/menor número
  - utiliza-se vetor
  - inserção nlog(n)
  - remoção nlog(n)

  Implementação em vetor
  - PARENT (i) = floor(i/2)
  - LEFT (i) = 2*i
  - RIGHT (i) = 2*i + 1

  Heap local

*/

int main(){
}
